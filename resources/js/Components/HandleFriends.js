import React, { useEffect } from "react";

export const HandleFriends = ({ user }) => {
    useEffect(() => {}, []);
    return <div>FriendList</div>;
};

// export default React.memo(HandleFriends);
